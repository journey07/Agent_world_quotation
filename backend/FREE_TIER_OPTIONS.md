# 무료 플랜 사용 가이드

## 문제점

### Vercel Hobby 플랜 (무료) 제한사항

**Serverless Functions 제한:**
- **기본 타임아웃**: 10초
- **최대 설정 가능 타임아웃**: **60초** (설정 필요)
- **메모리**: 최대 2GB
- **함수 호출**: 100만 회/월 포함
- **실행 시간**: 100 GB-hours/월 포함
- **CPU 시간**: 4 CPU-hours/월 포함

**중요**: Static Sites는 완전 무료이지만, **Serverless Functions는 별도 제한**이 있습니다.

### 3D 이미지 생성 문제

3D 이미지 생성은 Gemini API 호출로 **30초~2분 이상** 소요됩니다.
- Hobby 플랜 최대 60초로는 **부족할 수 있습니다**
- 60초 내에 완료되지 않으면 타임아웃 발생

## 해결 방법

### 옵션 1: 3D 생성만 별도 서버로 분리 (추천) 🆓

**가장 실용적인 무료 솔루션**

#### Render 사용 (무료 티어) ✅

1. **Render 계정 생성**: https://render.com
2. **무료 티어 제공**:
   - **750시간/월** (약 31일 × 24시간 = 744시간이므로 거의 24/7 가능)
   - **HTTP 요청 타임아웃: 최대 100분!** ⭐ (3D 이미지 생성에 충분)
   - 자동 배포
   - **주의사항**:
     - 15분 비활성 시 spin-down (cold start ~1분)
     - **✅ 해결 방법**: UptimeRobot 등으로 5-10분마다 ping → Cold Start 없음!
     - 파일 시스템은 ephemeral (재시작 시 사라짐)
     - 750시간 초과 시 다음 달까지 일시 중지

**Cold Start 피하는 방법:**
- **UptimeRobot** (무료): 5분마다 자동 ping → Cold Start 완전 방지 ⭐
- 자세한 내용: `RENDER_COLD_START_SOLUTION.md` 참고

3. **배포 방법**:
   ```bash
   # Render에서 새 Web Service 생성
   # Build Command: npm install
   # Start Command: node src/index.js
   ```

4. **프론트엔드 수정**:
   ```javascript
   // 3D 생성만 Render 서버 사용
   const API_3D_URL = 'https://your-render-app.onrender.com/api/quote/generate-3d-installation';
   
   // 나머지는 Vercel 사용
   const API_URL = 'https://your-vercel-app.vercel.app/api/quote';
   ```

#### Railway 사용 (무료 티어)

1. **Railway 계정 생성**: https://railway.app
2. **무료 크레딧**: $5/월
3. **배포**: GitHub 연결 후 자동 배포

#### Google Cloud Functions (무료 티어)

1. **무료 티어**: 200만 호출/월
2. **타임아웃**: 최대 540초
3. **배포**: `gcloud functions deploy`

### 옵션 2: 전체를 다른 플랫폼으로 이동

#### Render (무료) ✅
- 전체 백엔드를 Render에 배포
- **HTTP 요청 타임아웃: 최대 100분** (매우 충분!)
- 750시간/월 무료
- 15분 비활성 시 spin-down (cold start ~1분)

#### Fly.io (무료)
- 제한적이지만 무료 티어 제공
- 타임아웃 설정 가능

### 옵션 3: 비동기 처리 (복잡함)

**구현이 복잡하지만 무료로 가능:**

1. **요청 처리** (10초 이내):
   ```javascript
   // 작업 ID 생성 및 반환
   const jobId = generateJobId();
   await addToQueue(jobId, requestData);
   return { jobId, status: 'processing' };
   ```

2. **큐 시스템** (Upstash Redis 무료 티어):
   - 무료: 10,000 명령/일
   - 작업 저장

3. **워커 함수** (Vercel Cron 또는 외부):
   - 주기적으로 큐에서 작업 가져오기
   - Gemini API 호출
   - 결과를 DB/스토리지에 저장

4. **결과 조회**:
   ```javascript
   // 클라이언트에서 폴링
   const result = await fetch(`/api/quote/job-status/${jobId}`);
   ```

**단점:**
- 구현 복잡도 높음
- 클라이언트 코드 수정 필요
- 폴링으로 인한 추가 요청

## 비교표

| 방법 | 비용 | 타임아웃 | 구현 난이도 | 추천도 |
|------|------|---------|------------|--------|
| Vercel Pro 플랜 | $20/월 | 300초 (최대 800초) | ⭐ 쉬움 | ⭐⭐⭐⭐⭐ |
| **Render (3D만 분리)** | **무료** | **100분** ⭐ | ⭐⭐ 보통 | ⭐⭐⭐⭐⭐ |
| **전체 Render 이동** | **무료** | **100분** ⭐ | ⭐ 쉬움 | ⭐⭐⭐⭐ |
| Railway (3D만 분리) | 무료 | 제한적 | ⭐⭐ 보통 | ⭐⭐⭐ |
| 비동기 처리 | 무료 | - | ⭐⭐⭐⭐ 어려움 | ⭐⭐ |

## 추천 순서

1. **Render로 3D만 분리** ⭐ - 무료 + 타임아웃 100분 (가장 실용적!)
2. **전체 Render 이동** - 무료 + 타임아웃 100분 (Vercel 기능 포기하지만 완전 무료)
3. **Vercel Pro 플랜** - $20/월이지만 가장 간단하고 안정적
4. **비동기 처리** - 복잡하지만 무료

## 구현 예시: Render로 3D만 분리

### 1. Render에 배포

```bash
# render.yaml 생성
services:
  - type: web
    name: world-quotation-3d
    env: node
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: GEMINI_API_KEY
        sync: false
```

### 2. 프론트엔드 수정

```javascript
// App.jsx
const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3001/api/quote';
const API_3D_URL = import.meta.env.VITE_API_3D_URL || 'http://localhost:3001/api/quote';

// 3D 생성만 별도 URL 사용
const generate3D = async (imageData) => {
  const res = await fetch(`${API_3D_URL}/generate-3d-installation`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(imageData)
  });
  return res.json();
};
```

### 3. 환경 변수 설정

```bash
# .env
VITE_API_URL=https://your-vercel-app.vercel.app/api/quote
VITE_API_3D_URL=https://your-render-app.onrender.com/api/quote
```

## 결론

**무료로 사용하려면:**
- ✅ **Render로 3D 생성만 분리** (가장 실용적)
- ✅ **전체를 Render로 이동** (Vercel 포기)

**비용을 지불할 수 있다면:**
- ✅ **Vercel Pro 플랜** (가장 간단)
